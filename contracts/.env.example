# Sepolia RPC + Etherscan
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_KEY
ETHERSCAN_API_KEY=YOUR_KEY

# Platform: the wallet that pays for the resolver deploy.
# Only used by `forge script Bootstrap.s.sol`. Org admins act through the UI
# directly with their own wallets; no admin keys live here anymore.
PLATFORM_KEY=0x...
